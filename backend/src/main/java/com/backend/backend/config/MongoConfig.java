package com.backend.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MongoConfig {
    // Let Spring Boot auto-configure MongoClient from spring.mongodb.* properties.
}
